export const Underline = ({bottomMargin = "30px"}) => {
  return (
    <div
      style={{
        width: "100%",
        borderBottom: "2px solid var(--ifm-toc-border-color)",
        marginBottom: bottomMargin,
      }}
    ></div>
  );
};
